# Cartorio 404 — Static Mirror Site

Fictional multi-page static website inspired by **"The 404 Clerk's Office"**.

## Run locally

### Option 1 (direct)
Open `index.html` directly in your browser (`file://`).

### Option 2 (local static server)
From this folder, run:

```bash
python -m http.server 8000
```

Then open:

- `http://localhost:8000/index.html`

## Notes

- No external dependencies.
- No real data collection.
- No camera, fingerprinting, or external requests.
- Entirely fictional for horror-story presentation.
